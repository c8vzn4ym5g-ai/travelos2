# Acceptance Checklist

- `package.json` contains dev, build, lint, and typecheck scripts.
- App Router files exist under `app/`.
- Tailwind is imported by global CSS.
- Product docs exist under `docs/`.
- `docs/Tasks.md` contains `TASK-001`.
- Git is initialized.
- No secrets are committed.
